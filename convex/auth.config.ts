export default {
  providers: [
    {
      // Hardcoded — evaluated at deploy time, NOT runtime.
      domain: "https://animated-seahorse-414.convex.site",
      applicationID: "convex",
    },
  ],
};
